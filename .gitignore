# Rust build output (240 MB)
bridge/target/
# generated per-row module files, rewritten on every run
data/kb_mods/
data/tr_mods/
# third-party checkouts, cloned by setup.sh
data/KernelBench/
# corpora downloaded from HuggingFace, fetched by setup.sh
data/kernelbook_400.json
data/triton_traces.json
# intermediate per-run records; results/ holds the ones worth keeping
data/kb_*.json
data/kb_*.jsonl
__pycache__/
*.pyc

# third-party checkout fetched by setup.sh
data/KBV/

# raw console logs from a run; the artifacts are report.txt and verify.txt
results/*.log
results/*.done
# superseded records, kept locally for the before/after comparison only

# outreach drafts -- correspondence, not part of the artifact
outbox/
data/triton_multiturn.json
# superseded run records, kept locally for before/after comparisons only
data/archive/
results/archive/

# local virtualenv for running the checks (see requirements.txt)
.venv/

# which claims failed last time, for `verify.py --failed`
.verify-failed
